# The app runs in deterministic DEMO_MODE without any external credentials.
DEMO_MODE=true

# Optional future LLM enrichment. Never commit the real key.
LLM_API_KEY=
LLM_MODEL=
EMBEDDING_MODEL=

# Replit provides this for the built-in PostgreSQL database.
DATABASE_URL=